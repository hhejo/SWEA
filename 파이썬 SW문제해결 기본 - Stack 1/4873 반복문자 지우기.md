# 4873 반복문자 지우기



```python
for T in range(1, int(input()) + 1):
    stk = ['']
    for s in input():
        if s == stk[-1]:
            stk.pop()
        else:
            stk.append(s)
    print(f'#{T} {len(stk) - 1}')
```

